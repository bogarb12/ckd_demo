# WORKFLOW.md - กระบวนการทำงาน CKD Progression Dashboard

## 1. กระบวนการทำงานภาพรวม (Overall Workflow)

```
┌──────────────┐    ┌──────────────────┐    ┌───────────────────┐    ┌─────────────────┐
│  เปิดหน้าเว็บ  │──▶│  สร้างข้อมูลผู้ป่วย  │──▶│  แสดง Loading      │──▶│  Dashboard พร้อม  │
│  (Page Load)  │    │  1,530 ราย        │    │  Overlay          │    │  ใช้งาน           │
└──────────────┘    └──────────────────┘    └───────────────────┘    └─────────────────┘
```

---

## 2. ขั้นตอนการเริ่มต้นระบบ (Application Startup)

### ขั้นตอนที่ 1: โหลดทรัพยากร
```
Browser โหลด index.html
    ├── โหลด Google Fonts: Sarabun (ภาษาไทย)
    ├── โหลด css/style.css
    ├── โหลด Chart.js v4.4.7 จาก CDN
    └── โหลด JavaScript 4 ไฟล์:
        ├── js/data-generator.js
        ├── js/dashboard.js
        ├── js/overview.js
        └── js/individual.js
```

### ขั้นตอนที่ 2: เริ่มต้นแอปพลิเคชัน (DOMContentLoaded)
1. แสดง Loading Overlay (หน้าจอโหลด)
2. สร้าง `CKDDataGenerator` ด้วย seed = 42
3. เรียก `generatePatients(1530)` → ได้ข้อมูลผู้ป่วย 1,530 ราย
4. สร้าง `OverviewTab` และ `IndividualTab` class
5. เรียก `.render()` สำหรับทั้งสองแท็บ
6. ตั้งค่า Tab Navigation
7. ซ่อน Loading Overlay → ระบบพร้อมใช้งาน

### ขั้นตอนที่ 3: สร้างข้อมูลสังเคราะห์
```
CKDDataGenerator.generatePatients(1530)
    │
    ├── วนลูป 1,530 รอบ
    │   └── _generateOnePatient(i)
    │       ├── สร้างข้อมูลประชากร (ID, อายุ, เพศ, วันที่)
    │       ├── สร้างโรคร่วม (เบาหวาน, ความดัน, โรคหัวใจ)
    │       │   └── ความน่าจะเป็นเพิ่มตามอายุ
    │       ├── สร้างค่า Lab
    │       │   ├── eGFR ← ปรับตามโรคร่วมและอายุ
    │       │   ├── Creatinine ← สัมพันธ์ผกผันกับ eGFR
    │       │   ├── FBS, HbA1c ← สัมพันธ์กับเบาหวาน
    │       │   ├── SBP, DBP ← สัมพันธ์กับความดันสูง
    │       │   ├── BMI ← Normal Distribution
    │       │   └── Urine Protein ← สัมพันธ์กับ eGFR
    │       ├── สร้างข้อมูลยา (ACE Inhibitor, ARBs)
    │       ├── คำนวณ Outcome (CKD Progression 1 ปี)
    │       │   └── ความน่าจะเป็นจากปัจจัยเสี่ยงรวม
    │       └── ใส่ Flag คุณภาพข้อมูล (duplicate, outlier)
    │
    ├── เพิ่มข้อมูลซ้ำ ~2%
    └── Return patients[] array
```

---

## 3. กระบวนการทำงานแท็บภาพรวม (Overview Tab Workflow)

```
ผู้ใช้คลิกแท็บ "ภาพรวม"
    │
    ▼
┌────────────────────────────┐
│  OverviewTab.render()      │
│                            │
│  1. updateSummaryCards()   │──▶ คำนวณสถิติ 6 ค่า
│  2. renderCKDStageChart()  │──▶ กราฟ Doughnut: จำนวนผู้ป่วยแต่ละ Stage
│  3. renderRiskFactorChart()│──▶ กราฟ Bar: อัตรา Progression ตามปัจจัยเสี่ยง
│  4. renderEgfrDistChart()  │──▶ กราฟ Stacked Bar: การกระจาย eGFR
│  5. renderAgeEgfrScatter() │──▶ กราฟ Scatter: อายุ vs eGFR
│  6. renderBPScatter()      │──▶ กราฟ Scatter: SBP vs DBP
│  7. renderUrineProteinChart│──▶ กราฟ Stacked Bar: โปรตีนในปัสสาวะ
└────────────────────────────┘
```

**การโต้ตอบของผู้ใช้:**
- Hover บนกราฟ → แสดง Tooltip ข้อมูลละเอียด
- กราฟเป็นแบบอ่านอย่างเดียว (Read-only)
- ข้อมูลคำนวณครั้งเดียวตอน Render

---

## 4. กระบวนการทำงานแท็บรายบุคคล (Individual Tab Workflow)

### 4.1 การค้นหาผู้ป่วย

```
ผู้ใช้พิมพ์รหัสผู้ป่วย (เช่น "P001")
    │
    ▼
┌────────────────────────────────┐
│  Event: input → applyFilters() │
│                                │
│  1. อ่านค่าจากช่องค้นหา        │
│  2. อ่านค่าจากตัวกรอง 4 ชนิด   │
│  3. กรองข้อมูล (AND logic)     │
│  │   ├── Patient ID: substring match (case-insensitive)
│  │   ├── Stage: คำนวณจาก eGFR แล้วเทียบ
│  │   ├── Progression: เทียบค่า 0/1
│  │   ├── Gender: เทียบ M/F
│  │   └── Comorbidity: ตรวจ diabetes/hypertension/cvd
│  4. อัปเดต filteredPatients[]  │
│  5. เรียงลำดับ                  │
│  6. รีเซ็ตเป็นหน้า 1           │
│  7. แสดงตาราง + Pagination     │
│  8. อัปเดตจำนวนที่พบ            │
└────────────────────────────────┘
```

### 4.2 การเรียงลำดับ

```
ผู้ใช้คลิกหัวคอลัมน์ (เช่น "eGFR")
    │
    ▼
┌──────────────────────────────┐
│  sortBy(column)              │
│                              │
│  1. คอลัมน์เดิม? → สลับทิศทาง │
│     (ascending ↔ descending)│
│  2. คอลัมน์ใหม่? → ascending  │
│  3. sortPatients()           │
│     ├── ค่า null → เรียงไว้ท้าย │
│     ├── String → localeCompare│
│     └── Number → เปรียบเทียบตัวเลข
│  4. อัปเดตลูกศร (↑/↓)        │
│  5. แสดงตารางใหม่             │
└──────────────────────────────┘
```

### 4.3 การแบ่งหน้า (Pagination)

```
ผู้ใช้คลิกปุ่มหน้า (เช่น หน้า 3)
    │
    ▼
┌──────────────────────────────┐
│  1. currentPage = 3          │
│  2. คำนวณ slice:             │
│     start = (3-1) × 50 = 100│
│     end = 100 + 50 = 150     │
│  3. แสดงผู้ป่วยลำดับ 101-150 │
│  4. อัปเดตปุ่ม Pagination     │
│     ├── ปุ่ม Prev/Next       │
│     ├── ปุ่มหน้า (แสดงสูงสุด 5)│
│     └── จุดไข่ปลา (...) ถ้ามีช่องว่าง
└──────────────────────────────┘
```

### 4.4 การดูรายละเอียดผู้ป่วย

```
ผู้ใช้คลิกแถวผู้ป่วย (เช่น P00550)
    │
    ▼
┌──────────────────────────────────────────┐
│  selectPatient("P00550")                 │
│                                          │
│  1. ค้นหาข้อมูลผู้ป่วยจาก patients[]     │
│  2. ไฮไลท์แถวที่เลือก                    │
│  3. renderPatientDetail(patient)         │
│     │                                    │
│     ├── ส่วนหัว: ID, Badge, ข้อมูลส่วนตัว │
│     ├── CKD Stage Indicator (วงกลมสี)    │
│     ├── การประเมินความเสี่ยง              │
│     │   └── คำนวณคะแนน 0-13             │
│     ├── สัญญาณชีพ (SBP, DBP, BMI)       │
│     ├── ผล Lab (Creatinine, eGFR, FBS, HbA1c) │
│     ├── โปรตีนในปัสสาวะ                   │
│     ├── โรคร่วม & ยา                     │
│     └── ข้อมูลติดตาม                     │
│  4. renderRadarChart(patient)            │
│     │                                    │
│     ├── แปลงค่า 6 ตัวแปร → มาตราส่วน 0-100│
│     │   ├── eGFR (ผกผัน: สูง = ดี)       │
│     │   ├── Creatinine (ปกติ: 0-1.3)     │
│     │   ├── HbA1c (ปกติ: < 5.7%)        │
│     │   ├── SBP (ปกติ: < 130)            │
│     │   ├── DBP (ปกติ: < 80)             │
│     │   └── BMI (ปกติ: 18.5-24.9)       │
│     ├── คำนวณค่าเฉลี่ยความกังวล           │
│     └── เลือกสีกราฟ:                     │
│         ├── แดง: ค่าเฉลี่ย > 50%         │
│         ├── เหลือง: 25-50%              │
│         └── น้ำเงิน: < 25%              │
└──────────────────────────────────────────┘
```

---

## 5. กระบวนการทำงานแท็บคุณภาพข้อมูล (Data Quality Tab Workflow)

```
ผู้ใช้คลิกแท็บ "คุณภาพข้อมูล" (ครั้งแรก)
    │
    ▼
┌──────────────────────────────────────────┐
│  ตรวจสอบ qualityRendered flag            │
│  (false = ยังไม่เคยแสดง)                  │
│                                          │
│  renderDataQuality()                     │
│  │                                       │
│  ├── 1. กราฟข้อมูลสูญหาย                  │
│  │   ├── วิเคราะห์ 9 ฟิลด์               │
│  │   ├── คำนวณ % สูญหายแต่ละฟิลด์        │
│  │   └── แสดงกราฟแท่งแนวนอน              │
│  │       (แดง >25%, เหลือง 10-25%, เขียว <10%)
│  │                                       │
│  ├── 2. สถิติคุณภาพ (8 เมตริก)           │
│  │   ├── จำนวนผู้ป่วยทั้งหมด              │
│  │   ├── จำนวนข้อมูลซ้ำ                  │
│  │   ├── FBS Outliers                    │
│  │   ├── Age Outliers                    │
│  │   ├── สัดส่วนหน่วย Creatinine         │
│  │   ├── Class Balance                   │
│  │   └── Records with Outcome            │
│  │                                       │
│  └── 3. กราฟ Feature Distribution        │
│      ├── วิเคราะห์ 6 ตัวแปร               │
│      │   (Age, eGFR, SBP, DBP, BMI, FBS)│
│      ├── คำนวณ Min, Q25, Mean, Q75, Max  │
│      └── แสดงกราฟ Stacked Bar            │
│                                          │
│  ตั้งค่า qualityRendered = true           │
│  (ไม่ Render ซ้ำเมื่อกลับมาดูอีกครั้ง)      │
└──────────────────────────────────────────┘
```

---

## 6. กระบวนการสลับแท็บ (Tab Navigation)

```
ผู้ใช้คลิกปุ่มแท็บ
    │
    ▼
┌────────────────────────────────────┐
│  setupTabs() Event Handler         │
│                                    │
│  1. ลบ class "active" จากทุกปุ่ม   │
│  2. เพิ่ม class "active" ปุ่มที่คลิก │
│  3. ซ่อนทุก .tab-panel             │
│  4. แสดง Panel ที่ตรงกับปุ่ม        │
│  5. ถ้าเป็นแท็บ "คุณภาพข้อมูล"      │
│     └── ตรวจสอบ & Lazy Load        │
└────────────────────────────────────┘
```

---

## 7. ระบบสี Status (Color Coding System)

### สีสถานะค่า Lab

| สถานะ | สี | CSS Class | เงื่อนไข |
|--------|-----|-----------|----------|
| ปกติ (Normal) | เขียว | `status-normal` | ค่าอยู่ในเกณฑ์ปกติ |
| เฝ้าระวัง (Warning) | เหลือง | `status-warning` | ค่าเริ่มผิดปกติ |
| อันตราย (Danger) | แดง | `status-danger` | ค่าผิดปกติมาก |
| ไม่มีข้อมูล (Missing) | เทา | `status-missing` | ค่าเป็น null |

### ตัวอย่างเกณฑ์สี

**eGFR (mL/min/1.73m²):**
- เขียว: ≥ 60
- เหลือง: 30-59
- แดง: < 30

**SBP (mmHg):**
- เขียว: < 130
- เหลือง: 130-139
- แดง: ≥ 140

**HbA1c (%):**
- เขียว: < 5.7
- เหลือง: 5.7-6.4
- แดง: ≥ 6.5

---

## 8. Data Flow Diagram

```
                    ┌─────────────────────┐
                    │    Page Load         │
                    └──────────┬──────────┘
                               │
                    ┌──────────▼──────────┐
                    │  CKDDataGenerator    │
                    │  seed = 42           │
                    │  1,530 patients      │
                    └──────────┬──────────┘
                               │
                    ┌──────────▼──────────┐
                    │  patients[] (Memory) │
                    └──┬──────┬──────┬────┘
                       │      │      │
           ┌───────────┘      │      └───────────┐
           ▼                  ▼                   ▼
   ┌───────────────┐ ┌───────────────┐  ┌─────────────────┐
   │  Overview Tab  │ │ Individual Tab│  │ Data Quality Tab │
   │               │ │               │  │                  │
   │ patients[]    │ │ patients[]    │  │ patients[]       │
   │ → สถิติ       │ │ → filter[]   │  │ → วิเคราะห์       │
   │ → กราฟ 6 ชนิด │ │ → sort       │  │ → กราฟ 3 ชนิด    │
   │               │ │ → paginate   │  │ → สถิติคุณภาพ     │
   │               │ │ → detail     │  │                  │
   │               │ │ → radar      │  │                  │
   └───────┬───────┘ └──────┬───────┘  └────────┬─────────┘
           │                │                    │
           ▼                ▼                    ▼
   ┌─────────────────────────────────────────────────────┐
   │                Chart.js v4.4.7                      │
   │  Doughnut │ Bar │ Scatter │ Stacked Bar │ Radar    │
   └─────────────────────────────────────────────────────┘
           │                │                    │
           ▼                ▼                    ▼
   ┌─────────────────────────────────────────────────────┐
   │                    DOM (HTML)                        │
   │        ผู้ใช้เห็นและโต้ตอบกับหน้าเว็บ                  │
   └─────────────────────────────────────────────────────┘
```

---

## 9. สรุปกระบวนการทำงานหลัก

| ลำดับ | กระบวนการ | ไฟล์ที่เกี่ยวข้อง | ผลลัพธ์ |
|-------|----------|-----------------|---------|
| 1 | โหลดหน้าเว็บ | index.html | โครงสร้าง DOM พร้อม |
| 2 | สร้างข้อมูลสังเคราะห์ | data-generator.js | patients[] 1,530 ราย |
| 3 | เริ่มต้นระบบ | dashboard.js | Tab Navigation พร้อม |
| 4 | แสดงภาพรวม | overview.js | สถิติ + กราฟ 6 ชนิด |
| 5 | แสดงรายบุคคล | individual.js | ตาราง + ตัวกรอง |
| 6 | ค้นหา/กรองผู้ป่วย | individual.js | filteredPatients[] |
| 7 | ดูรายละเอียด | individual.js | Detail Panel + Radar Chart |
| 8 | ดูคุณภาพข้อมูล | dashboard.js | กราฟ + สถิติคุณภาพ |
| 9 | สลับแท็บ | dashboard.js | เปลี่ยน Panel ที่แสดง |
